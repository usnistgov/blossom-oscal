- uuid: {{ uuid.control }}
  control-id: sc-7.3
  statements: 
    - statement-id: sc-7.3
      uuid: {{ uuid.statement }}
      by-components:
        - component-uuid: {{ uuid.component-uuid }}
          uuid: {{ uuid.by-component }}
          description: >-
            There are no external network connections to the system. Connections are only allowed through the internal network.
