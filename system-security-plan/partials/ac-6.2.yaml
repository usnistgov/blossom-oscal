control-id: ac-6.2
  set-parameters:
    - param-id: ac-6.2_prm_1
      values: >-
        Establish system accounts, configure access authorizations (i.e., permissions, privileges), configure settings for events to be audited, and establish intrusion detection parameters or have acces to access control lists.
  
  statements:
    - statement-id: ac-6.2
      uuid: {{ uuid.statement }}
      by-components:
        - component-uuid: {{ uuid.component-uuid }}
          uuid: {{ uuid.by-component }}
          description: >-
            Users of system accounts (or roles) with access to establish system accounts, configure access authorizations (i.e., permissions, privileges), configure settings for events to be audited, and establish intrusion detection parameters or have access to access control lists will use non-privileged accounts or roles when accessing non-security functions.      
